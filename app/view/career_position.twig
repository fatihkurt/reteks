{% extends "layout/main.layout.twig" %}


{% block content %}

        {% if item.page.image1 != '' %}
        <div class="banner">
            <img src="/upload/page/{{ item.page.image1 }}" />
        </div>
        {% endif %}
        
        
        <div class="middle-content">

            <div class="page-content">
        
                <h3 class="page-title">{{ mb_strtoupper(item.title) }}</h3>

                {% include 'partial/breadjump.twig' %}

                <ul class="position-list">
                    {% for position in positions %}
                        <li>
                                <a class="position-btn" href="/{{ lang }}/{{ applicarion_url }}?is={{ position.id }}">{{ T.career_application }}</a>
                                <h4 class="position-text">{{ position.getName(lang) }}</h4>
                                <div class="position-firm">R&E Teks</div>
                                <a href="#" class="position-detail-btn">{{ T.career_detail_info }}</a>
                                <div class="position-detail">{{ position.getDetail(lang)|raw }}</div>
                        </li>
                    {% endfor %}
                    </ul>

            </div>

        </div> <!-- #middle-content -->
        
        
        <div class="middle-right">
            
            <div class="right-col news page-list">
                <ul>
                    {% for cpage in cpages %}
                    <li {% if item.page_id == cpage.id  %}class="s-cpage"{% endif %}>
                        <a href="/{{lang}}/{{ cpage.content(lang).seo_url }}">{{ cpage.content(lang).title }}</a>
                    </li>
                    {% endfor %}
                </ul>
            </div>
        
            {% include 'partial/video_play.twig' %}
            
            {% include 'partial/newsletter.twig' %}
        </div>
{% endblock %}
